<h1>Edit Review for <%= @course[:name] %></h1>

<form action="/reviews/<%= @review[:id] %>/update" method="post">
  <div class="form-group">
    <label>Would You Recommend??</label>
    <select class="form-control" name="going">
      <% if @review[:going] %>
        <option value="1" selected>Yes</option>
        <option value="0">No</option>
      <% else %>
        <option value="1">Yes</option>
        <option value="0" selected>No</option>
      <% end %>
    </select>
  </div>

  <div class="form-group">
    <label>Comments</label>
    <textarea class="form-control" rows="3" name="comments"><%= @rsvp[:comments] %></textarea>
  </div>
  <button type="submit" class="btn btn-primary">Submit</button>
</form>